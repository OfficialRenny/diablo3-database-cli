// var request = require('request');

// var config = require('./config_en');

var d3client = {};

// @param cid - Class ID.
// @param isActive - true for active skills, flase for passive skills.
d3client.getSkills = function(cid, isActive) {
};

// @param cid - Class ID.
// @param sid - Skill ID.
d3client.getSkill = function(cid, sid) {
};

module.exports = d3client;
